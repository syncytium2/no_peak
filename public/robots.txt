# no_peak — https://nopeak.tonydefazio.com
#
# All crawlers are welcome, explicitly including AI/LLM crawlers (GPTBot,
# ClaudeBot, PerplexityBot, Google-Extended, CCBot, and peers): this is a free
# public scientific tool and we want it discoverable and described accurately.
# The same decision, for the same reasons, as its sibling Colonel Kernel.
#
# A curated summary for language models is at /llms.txt; the citable methods
# reference is at /methods, which is real HTML and needs no JavaScript. The app
# itself sends no data anywhere — no backend, no analytics — so crawling it
# cannot expose any user's data.
#
# This policy IS in effect as of 2026-08-13, checked on the wire. It was NOT
# from 2026-08-10 to 2026-08-13, and nothing in this repository said so, which
# is why the following is written down rather than deleted: two Cloudflare edge
# features silently void this file, and both are dashboard settings on the
# tonydefazio.com zone that no file here can override.
#
# 1. "Managed robots.txt" (AI Crawl Control). PREPENDS a managed block with
#    `Content-Signal: ai-train=no` and per-agent `Disallow: /` for GPTBot,
#    ClaudeBot, CCBot, Google-Extended, and peers. A named user-agent group
#    beats our `User-agent: *` group, so the crawlers we mean to allow are
#    blocked regardless of what this file says. It was ON from 2026-08-10 and
#    was turned OFF on 2026-08-13.
# 2. "Block AI Scrapers and Crawlers" (Security > Bots) acts at the edge BEFORE
#    robots.txt is ever read. It has been off throughout. Leave it off.
#
# VERIFYING TAKES TWO CHECKS, because those two fail differently and a clean
# robots.txt proves nothing about an edge block sitting in front of it:
#
#   curl -s https://nopeak.tonydefazio.com/robots.txt
#     — this file's text should be the WHOLE response, no managed preamble.
#   curl -sI -A 'Mozilla/5.0 (compatible; ClaudeBot/1.0)' \
#        https://nopeak.tonydefazio.com/methods
#     — must be 200, not 403.

User-agent: *
Allow: /

Sitemap: https://nopeak.tonydefazio.com/sitemap.xml
