{
  "name": "no_peak",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "description": "Client-side CLUSTER pulse detection for time-series hormone sampling data",
  "scripts": {
    "dev": "vite",
    "cluster": "node scripts/cluster.ts",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "deploy": "npm run test && npm run build && wrangler deploy"
  },
  "dependencies": {
    "jspdf": "^4.2.1",
    "jspdf-autotable": "^5.0.8",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "svg2pdf.js": "^2.7.0"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "playwright-core": "1.62.1",
    "typescript": "^5.6.3",
    "vite": "^6.0.3",
    "vitest": "^2.1.8",
    "wrangler": "^4.0.0"
  }
}
